@if (ViewData["PreRenderEnabled"] is bool && (bool)ViewData["PreRenderEnabled"])
{
    @Html.Raw(ViewData["SpaHtml"])
}
else
{
    <app></app>
}
@section Scripts {
    @{ await Html.RenderPartialAsync("_ScriptsPartial"); }
}
